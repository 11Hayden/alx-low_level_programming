#include "main.h"

/**
* _isdigit - to check for digit
* @c: integer c
* Return: returns 1 if uppercase
*/

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
